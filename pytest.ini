[pytest]
testpaths = src/tests

# src/sandbox contains standalone GL scratch scripts whose names begin with test_ but
# which are not tests - they create GL contexts and do work at import time.
norecursedirs = src/sandbox build dist .git __pycache__
